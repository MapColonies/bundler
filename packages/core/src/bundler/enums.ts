export enum TaskStage {
  BUILDING = 'building',
  PULLING = 'pulling',
  SAVING = 'saving',
  DOWNLOADING = 'downloading',
}

export enum BundleDirs {
  IMAGES = 'images',
  HELM_PACKGES = 'helm',
  ASSETS = 'assets',
}
